<Page x:Class="_301._3_Development.Pages.AdminPages.AdminLogsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:_301._3_Development.Pages.AdminPages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AdminLogsPage">

    <Grid Margin="20" Background="White">
        <DockPanel>
            <TextBlock DockPanel.Dock="Top" x:Name="Header"
                       FontSize="24"
                       FontWeight="Bold"
                       Margin="0 0 0 20"/>

            <ListBox x:Name="LogsList" Margin="20">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <TextBlock Text="{Binding Timestamp, StringFormat=yyyy-MM-dd HH:mm:ss}" FontWeight="Bold"/>
                            <TextBlock Text="{Binding Action}" TextWrapping="Wrap"/>
                            <TextBlock Text="{Binding UserID}" FontStyle="Italic" FontSize="12"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </DockPanel>
    </Grid>
</Page>

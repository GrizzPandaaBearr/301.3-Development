<Page x:Class="_301._3_Development.Pages.PatientPages.PatientBookAppointmentPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:_301._3_Development.Pages.PatientPages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PatientBookAppointmentPage">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Book Appointment"
                   FontSize="24"
                   FontWeight="Bold"
                   Margin="0 0 0 20"/>

        <StackPanel Grid.Row="1">
            <TextBlock Text="Choose Doctor:"/>
            <ComboBox x:Name="DoctorComboBox"
                      DisplayMemberPath="Name"
                      Margin="0 0 0 10" />

            <TextBlock Text="Select Date:"/>
            <DatePicker x:Name="DatePicker"
                        Margin="0 0 0 10"/>

            <TextBlock Text="Reason:"/>
            <TextBox x:Name="ReasonTextBox"
                     Height="80"
                     TextWrapping="Wrap"
                     AcceptsReturn="True"/>
        </StackPanel>

        <Button Content="Book Appointment"
                Grid.Row="3"
                Height="40"
                Margin="0 20 0 0"
                VerticalAlignment="Bottom"
                Click="BookAppointment_Click"/>
    </Grid>
</Page>
